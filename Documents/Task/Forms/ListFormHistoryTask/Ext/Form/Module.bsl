
&AtServer
Procedure OnCreateAtServer(Cancel, StandardProcessing)
	
	Если НЕ Параметры.Свойство("Outlet") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Параметры.Outlet) Тогда
		List.Filter.Items[0].RightValue =  Параметры.Outlet;
		//Список.Отбор.Элементы[1].Использование = Истина;
		//Список.Отбор.Элементы[0].ПравоеЗначение = СписокНедоступныхПрофилей;
	КонецЕсли;
	
EndProcedure
